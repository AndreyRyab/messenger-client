import type { StatusType } from '../types';

export const IN_PROGRESS: StatusType = 'IN_PROGRESS';
export const SUCCESS: StatusType = 'SUCCESS';
export const FAILED: StatusType = 'FAILED';


export const CONNECTED = 'CONNECTED';
export const DISCONNECTED = 'DISCONNECTED';
